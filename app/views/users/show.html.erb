<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %> <br/><br/>
        <%= @user.name %>
      </h1>
    </section>
    <h4> 
     	<%= @user.email %><br>
       <%= @user.created_at %><br/>
  		<%= @user.password_digest %></h4>
  </aside>
</div>